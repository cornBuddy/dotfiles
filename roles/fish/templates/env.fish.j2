#!/usr/bin/fish

{{ ansible_managed | comment }}

set -x EDITOR nvim
set -x PAGER less
set -x GOROOT "{{ fish_go_root }}"
set -x GOPATH "{{ fish_go_path }}"
set PATH $PATH "{{ fish_local_bin_path }}" "$GOROOT/bin" "$GOPATH/bin"
