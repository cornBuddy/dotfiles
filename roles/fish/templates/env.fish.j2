#!/usr/bin/fish

{{ ansible_managed | comment }}

set -x EDITOR nvim
set -x PAGER less
set -x GOROOT "{{ zsh_go_root }}"
set -x GOPATH "{{ zsh_go_path }}"
set PATH $PATH "{{ zsh_local_bin_path }}" "$GOROOT/bin" "$GOPATH/bin"
