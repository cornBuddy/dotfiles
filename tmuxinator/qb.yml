name: qb
root: ~/workspace/quotebuilder/QB2/QuoteBuilder.Web
socket_name: default
windows:
    - editor: nvim .
    - server:
        layout: even-horizontal
        panes:
            - npm run webpack-dev-server
            - htop
